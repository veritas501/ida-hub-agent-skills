// 自动生成 — 由 scripts/embed-web.ts 在构建时产出
// 此为空模板，仅用于开发模式；bundle:bin 时会被真实内容覆盖
export const embeddedWebAssets: Record<string, string> = {};

export function isBase64(_content: string): boolean {
  return false;
}

export function decodeBase64(_content: string): Uint8Array {
  return new Uint8Array(0);
}
