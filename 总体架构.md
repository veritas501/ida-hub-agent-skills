# IDA Chat 分布式架构改造方案（FastAPI + Next.js 版）

## Context（背景）

将 IDA Chat 改造为分布式架构，支持**多个 IDA 实例**的统一管理：
- **IDA Pro** 运行在 Windows 主机上（可能有多个实例）
- **Claude Code** 运行在 Linux 主机上
- **Hub Server** 使用 FastAPI + Next.js，绑定 0.0.0.0，局域网直接访问
- **Claude Code** 通过 curl/python 命令与 HTTP API 交互（不使用 MCP）

---

## 目标架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    Windows 主机 (局域网 0.0.0.0)                             │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                    IDA Hub Server (FastAPI + Next.js)                 │  │
│  │                                                                       │  │
│  │  ┌─────────────────────────┐    ┌─────────────────────────────────┐  │  │
│  │  │  Next.js Frontend       │    │  FastAPI Backend                │  │  │
│  │  │  (管理界面)              │    │  (HTTP API)                     │  │  │
│  │  │  :3000                  │    │  :10086                          │  │  │
│  │  │                         │    │                                 │  │  │
│  │  │  • IDA 实例列表         │    │  • /api/instances               │  │  │
│  │  │  • 连接状态             │    │  • /api/execute                 │  │  │
│  │  │  • 配置复制             │    │  • /api/db-info                 │  │  │
│  │  │  • 日志查看             │    │  • WebSocket /ws                │  │  │
│  │  └─────────────────────────┘    └─────────────────────────────────┘  │  │
│  │                                       ↑                               │  │
│  │                        InstanceRegistry                               │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                        ↑                                    │
│            ┌───────────────────────────┼───────────────────────────┐        │
│            │ WebSocket                 │ WebSocket                 │        │
│            ▼                           ▼                           ▼        │
│  ┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐    │
│  │   IDA 实例 1     │      │   IDA 实例 2     │      │   IDA 实例 N     │    │
│  │  (calc.exe.i64)  │      │  (malware.dll)  │      │  (kernel.exe)   │    │
│  └─────────────────┘      └─────────────────┘      └─────────────────┘    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                         │
                                   局域网 HTTP
                                         │
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Linux 主机                                         │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                      Claude Code (Skill)                               │  │
│  │                                                                       │  │
│  │  通过 curl/python 命令调用 Hub API:                                    │  │
│  │                                                                       │  │
│  │  # 列出实例                                                           │  │
│  │  curl http://192.168.x.x:10086/api/instances                           │  │
│  │                                                                       │  │
│  │  # 执行脚本                                                           │  │
│  │  curl -X POST http://192.168.x.x:10086/api/execute \                  │  │
│  │    -H "Content-Type: application/json" \                              │  │
│  │    -d '{"instance_id": "xxx", "code": "..."}'                        │  │
│  │                                                                       │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## HTTP API 设计（FastAPI）

### 端点

| 方法 | 端点 | 说明 |
|------|------|------|
| `GET` | `/api/instances` | 列出所有已连接的 IDA 实例 |
| `POST` | `/api/execute` | 在指定实例执行 Python 代码 |
| `GET` | `/api/db-info/{instance_id}` | 获取数据库信息 |
| `GET` | `/api/config` | 获取 Claude Code 配置（可复制） |
| `WS` | `/ws` | IDA 实例 WebSocket 连接 |

### API Schema

```python
# GET /api/instances
{
  "instances": [
    {
      "instance_id": "a1b2c3d4",
      "module": "calc.exe",
      "db_path": "C:\\analyses\\calc.exe.i64",
      "architecture": "x86_64",
      "functions_count": 150,
      "connected_at": "2026-03-12T10:30:00Z"
    }
  ]
}

# POST /api/execute
# Request:
{
  "instance_id": "a1b2c3d4",
  "code": "for func in db.functions:\n    print(func.start_ea)"
}
# Response:
{
  "success": true,
  "output": "0x140001000\n0x140001100\n...",
  "error": null
}

# GET /api/db-info/{instance_id}
{
  "instance_id": "a1b2c3d4",
  "module": "calc.exe",
  "path": "C:\\analyses\\calc.exe.i64",
  "architecture": "x86_64",
  "bitness": 64,
  "base_address": "0x140000000",
  "functions_count": 150,
  "strings_count": 500,
  "segments": [".text", ".data", ".rdata"]
}

# GET /api/config
{
  "hub_url": "http://192.168.1.100:10086",
  "curl_examples": {
    "list_instances": "curl http://192.168.1.100:10086/api/instances",
    "execute": "curl -X POST http://192.168.1.100:10086/api/execute -H 'Content-Type: application/json' -d '{\"instance_id\": \"INSTANCE_ID\", \"code\": \"print(len(db.functions))\"}'"
  }
}
```

---

## 前端设计（Next.js）

### 页面

| 页面 | 路由 | 功能 |
|------|------|------|
| Dashboard | `/` | 实例列表、状态概览 |
| Instance Detail | `/instance/[id]` | 单个实例详情、脚本执行 |
| Config | `/config` | Claude Code 配置复制 |

### Dashboard 页面

```
┌─────────────────────────────────────────────────────────────────┐
│  IDA Hub Server                              [Refresh] [Config] │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Connected Instances (2)                                        │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ 🟢 calc.exe.i64                                           │ │
│  │    ID: a1b2c3d4 | x86_64 | 150 functions                  │ │
│  │    Path: C:\analyses\calc.exe.i64                         │ │
│  │                                          [Execute] [Info] │ │
│  └───────────────────────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ 🟢 malware.dll                                            │ │
│  │    ID: e5f6g7h8 | x86 | 89 functions                      │ │
│  │    Path: C:\analyses\malware.dll.i64                      │ │
│  │                                          [Execute] [Info] │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  Server Info                                                    │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ Hub URL: http://192.168.1.100:10086                        │ │
│  │ Uptime: 2h 30m                                            │ │
│  │ Requests: 156                                              │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Config 页面（配置复制）

```
┌─────────────────────────────────────────────────────────────────┐
│  Claude Code Configuration                         [Copy All]  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Quick Commands (click to copy)                                 │
│                                                                 │
│  List instances:                                                │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ curl http://192.168.1.100:10086/api/instances              │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  Execute code (replace INSTANCE_ID):                            │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ curl -X POST http://192.168.1.100:10086/api/execute \      │ │
│  │   -H 'Content-Type: application/json' \                   │ │
│  │   -d '{"instance_id": "INSTANCE_ID", "code": "CODE"}'     │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  Python Helper (save as ida_client.py):                        │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ import requests                                            │ │
│  │ HUB_URL = "http://192.168.1.100:10086"                      │ │
│  │                                                            │ │
│  │ def list_instances():                                      │ │
│  │     return requests.get(f"{HUB_URL}/api/instances").json()│ │
│  │                                                            │ │
│  │ def execute(instance_id, code):                            │ │
│  │     return requests.post(f"{HUB_URL}/api/execute",        │ │
│  │         json={"instance_id": instance_id, "code": code}   │ │
│  │     ).json()                                               │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Claude Code Skill 设计

**文件: `~/.claude/skills/ida/skill.md`**

```markdown
# IDA Hub Skill

通过 HTTP API 与远程 IDA Hub Server 交互。

## 使用方式

/ida <command> [args]

## 命令

- `/ida list` - 列出所有已连接的 IDA 实例
- `/ida info <instance_id>` - 获取实例数据库信息
- `/ida exec <instance_id> <code>` - 执行 Python 代码

## 示例

/ida list
/ida info a1b2c3d4
/ida exec a1b2c3d4 "print(len(db.functions))"
/ida exec a1b2c3d4 "for func in db.functions:\n    print(db.functions.get_name(func))"
```

**Skill 实现** (通过 Bash 工具调用):

```python
# skill 执行逻辑
HUB_URL = "http://192.168.1.100:10086"  # 从环境变量或配置读取

# /ida list
curl -s http://$HUB_URL/api/instances | jq .

# /ida info <id>
curl -s http://$HUB_URL/api/db-info/$INSTANCE_ID | jq .

# /ida exec <id> <code>
curl -s -X POST http://$HUB_URL/api/execute \
  -H 'Content-Type: application/json' \
  -d "{\"instance_id\": \"$INSTANCE_ID\", \"code\": \"$CODE\"}"
```

---

## 文件变更清单

| 文件/目录 | 操作 | 说明 |
|-----------|------|------|
| `hub/` | **新增目录** | Hub Server 项目 |
| `hub/server/` | 新增 | FastAPI 后端 |
| `hub/server/main.py` | 新增 | FastAPI 应用入口 |
| `hub/server/registry.py` | 新增 | IDA 实例注册表 |
| `hub/server/routes.py` | 新增 | API 路由 |
| `hub/web/` | 新增 | Next.js 前端 |
| `hub/web/app/` | 新增 | Next.js App Router |
| `hub/web/app/page.tsx` | 新增 | Dashboard 页面 |
| `hub/web/app/config/page.tsx` | 新增 | Config 页面 |
| `ida_hub_client.py` | **新增** | IDA 客户端（连接 Hub） |
| `ida_chat_plugin.py` | **修改** | 添加 Hub 连接 UI |
| `pyproject.toml` | **修改** | 添加 FastAPI 依赖 |
| `~/.claude/skills/ida/skill.md` | **新增** | Claude Code Skill |

---

## 项目结构

```
ida-chat-plugin/
├── hub/                          # Hub Server 项目
│   ├── server/                   # FastAPI 后端
│   │   ├── __init__.py
│   │   ├── main.py               # 应用入口
│   │   ├── registry.py           # 实例注册表
│   │   ├── routes.py             # API 路由
│   │   └── ws_handler.py         # WebSocket 处理
│   ├── web/                      # Next.js 前端
│   │   ├── app/
│   │   ├── components/
│   │   ├── package.json
│   │   └── next.config.js
│   ├── pyproject.toml            # Python 依赖
│   └── run.py                    # 启动脚本
├── ida_hub_client.py             # IDA 客户端
├── ida_chat_plugin.py            # IDA 插件（修改）
├── ida_chat_core.py              # 核心模块（保留）
└── ...
```

---

## 实施阶段

### Phase 1: FastAPI 后端
- [ ] 创建 `hub/server/` 目录结构
- [ ] 实现 `registry.py`（实例注册表）
- [ ] 实现 `routes.py`（HTTP API）
- [ ] 实现 `ws_handler.py`（WebSocket 处理）
- [ ] 实现 `main.py`（FastAPI 应用）

### Phase 2: IDA 客户端
- [ ] 创建 `ida_hub_client.py`
- [ ] 实现 WebSocket 连接
- [ ] 实现脚本执行（execute_sync）
- [ ] 修改 `ida_chat_plugin.py` 添加 UI

### Phase 3: Next.js 前端
- [ ] 创建 `hub/web/` 项目
- [ ] 实现 Dashboard 页面
- [ ] 实现 Config 页面
- [ ] 实现实例详情页面

### Phase 4: Claude Code 集成
- [ ] 创建 Skill 文件
- [ ] 测试 curl 命令
- [ ] 编写使用文档

---

## 启动流程

```bash
# 1. 启动 Hub Server (Windows)
cd hub
python run.py --host 0.0.0.0 --port 10086

# 2. 启动前端开发服务器 (可选，生产环境可 build)
cd hub/web
npm run dev  # :3000

# 3. IDA 连接
#    在 IDA 插件中配置 Hub 地址，点击连接

# 4. Claude Code 使用
/ida list
/ida exec <id> "print(db.module)"
```

---

## 验证步骤

1. **启动 Hub Server**
   ```bash
   python hub/run.py
   # 访问 http://localhost:10086/docs 查看 API 文档
   ```

2. **IDA 连接测试**
   - 打开 IDA，加载插件
   - 点击 "Connect to Hub"
   - 检查前端页面是否显示实例

3. **API 测试**
   ```bash
   # 列出实例
   curl http://localhost:10086/api/instances

   # 执行脚本
   curl -X POST http://localhost:10086/api/execute \
     -H 'Content-Type: application/json' \
     -d '{"instance_id": "xxx", "code": "print(len(db.functions))"}'
   ```

4. **前端测试**
   - 访问 http://localhost:3000
   - 检查实例列表显示
   - 测试配置复制功能

---

## 依赖

### Python (FastAPI)
```toml
dependencies = [
    "fastapi>=0.109.0",
    "uvicorn[standard]>=0.27.0",
    "websockets>=12.0",
    "pydantic>=2.0.0",
]
```

### Node.js (Next.js)
```json
{
  "dependencies": {
    "next": "^14.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  }
}
```
